# homebrew-tap

```sh
brew install --cask geetpurwar/tap/lidguard
```

The cask's `uninstall` stanza also removes the optional deep-hold LaunchDaemon, so
`brew uninstall --cask lidguard` leaves nothing privileged behind.
